import { StyleSheet, Text, View } from "react-native";
import React from "react";

interface Props {
  title: string;
}
const OnboardingScreenWrapper = () => {
  return (
    <View>
      <Text>OnboardingScreenWrapper</Text>
    </View>
  );
};

export default OnboardingScreenWrapper;

const styles = StyleSheet.create({});
